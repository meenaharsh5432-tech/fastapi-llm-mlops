global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "fastapi-llm"
    static_configs:
      # Service name when running via docker-compose; use localhost:8000 if running FastAPI locally
      - targets: ["fastapi:8000"]
    metrics_path: /metrics
